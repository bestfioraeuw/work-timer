# Loop context 002 — 2026-09-09 01:55

后续 tick **只读本文件 + `state.json`**。不要重扫仓库，不要读 DESIGN.md / index.html 全文 / 001。

## 任务与退出

自我迭代工作钟。唯一用户：28 岁大陆男工程师。上下班、工作/休息、日报，要顺手。

`/ponytail` full · UI 跟 DESIGN.md（`#0066cc`，无第二色/渐变/卡片阴影）· **真机 macOS** · **保持小巧**（零新依赖、不新页面、不托盘/打包）。

退出：同一问题调试失败 >5；或 `>= 2026-09-09T04:29:00+08:00`。退出后 commit 当前分支并 push。

Context ≥85%：写 `context-003.md` 再概括。

## 已落地（勿重做）

- 芯片换活、欢迎页「开始今天」、`/` 聚焦、校准折叠
- 系统字体（无 Google Fonts），Mac 用 SF Pro
- 昨天未收工：`open_prev` +「昨天还没下班」→ 23:59
- 下班时空日报写成事项顿号列表；已有手写不覆盖
- 不在输入框时：`/` 聚焦事项，`r` 休息，`o` 下班（无修饰键，Mac/Win 一样）
- 开工/换活后清空事项框

分支 `feature/loop`，改动未提交。测试：`python test_clock.py`。

## 下一轮只做一件

空芯片不要编假事项。下一刀看实际使用：文案、空状态，或停手。

不要做：新依赖、新颜色、新页面、示例芯片、菜单栏、只绑 Ctrl 的快捷键。
